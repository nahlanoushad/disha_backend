meta {
  name: Delete Course
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/courses/{{courseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

vars:pre-request {
  baseUrl: http://localhost:5000/api
  adminToken: REPLACE_WITH_VALID_ADMIN_TOKEN
  courseId: REPLACE_WITH_ACTUAL_COURSE_ID
}
