meta {
  name: Create Category - Unauthorized (No Token)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Secret Category",
    "description": "Should fail with 401"
  }
}

vars:pre-request {
  baseUrl: http://localhost:5000/api
}
