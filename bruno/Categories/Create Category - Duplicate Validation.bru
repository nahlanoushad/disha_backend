meta {
  name: Create Category - Duplicate Name Failure
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Engineering",
    "description": "This should fail because Engineering already exists from the seed"
  }
}

vars:pre-request {
  baseUrl: http://localhost:5000/api
}
